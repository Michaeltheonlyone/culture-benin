{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "touch database/database.sqlite && php artisan config:clear && php artisan cache:clear && php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": ""
  }
}